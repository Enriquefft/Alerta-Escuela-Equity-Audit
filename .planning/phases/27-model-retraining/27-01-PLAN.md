---
phase: 27-model-retraining
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/lightgbm_xgboost.py
  - src/models/calibration.py
  - data/processed/model_lgbm.joblib
  - data/processed/model_xgb.joblib
  - data/processed/model_lgbm_calibrated.joblib
  - data/processed/predictions_lgbm.parquet
  - data/processed/predictions_xgb.parquet
  - data/processed/predictions_lgbm_calibrated.parquet
  - data/exports/model_results.json
  - data/exports/onnx/lightgbm_dropout.onnx
  - data/exports/figures/pr_curve_lgbm.png
  - data/exports/figures/pr_curve_xgb.png
  - data/exports/figures/calibration_plot.png
autonomous: true
requirements: [MODEL-01, MODEL-03]

must_haves:
  truths:
    - "LightGBM retrained with Optuna (100 trials) using 31 features, best hyperparameters logged in model_results.json"
    - "XGBoost retrained with Optuna (50 trials) using 31 features"
    - "Primary LightGBM re-calibrated with Platt scaling, new A/B coefficients stored"
    - "ONNX re-exported from raw LightGBM, max diff < 1e-4 vs Python predictions"
    - "model_results.json updated with new lightgbm, xgboost, test_2023_calibrated entries showing 31 features"
  artifacts:
    - path: "data/processed/model_lgbm.joblib"
      provides: "Retrained LightGBM model (31 features)"
    - path: "data/processed/model_lgbm_calibrated.joblib"
      provides: "Re-calibrated LightGBM model"
    - path: "data/exports/onnx/lightgbm_dropout.onnx"
      provides: "ONNX export of retrained LightGBM"
    - path: "data/exports/model_results.json"
      provides: "Updated metrics for lightgbm, xgboost, test_2023_calibrated"
  key_links:
    - from: "src/models/lightgbm_xgboost.py"
      to: "src/data/features.py"
      via: "MODEL_FEATURES import"
      pattern: "from data.features import MODEL_FEATURES"
    - from: "src/models/calibration.py"
      to: "data/processed/model_lgbm.joblib"
      via: "joblib.load"
      pattern: "joblib.load.*model_lgbm"
---

<objective>
Retrain LightGBM and XGBoost with the expanded 31-feature matrix (was 25), re-calibrate the primary LightGBM with Platt scaling, and re-export ONNX.

Purpose: The feature matrix expanded from 25 to 31 features in Phase 26 (overage-for-grade, interaction terms). All model artifacts (joblibs, predictions, ONNX, model_results.json) must be regenerated with the new features. This is the foundation for subsequent fairness re-analysis.

Output: Updated model artifacts (joblibs, predictions parquets, ONNX, PR curves, calibration plot) and model_results.json with new LightGBM/XGBoost/calibrated entries.
</objective>

<execution_context>
@/home/hybridz/.claude/get-shit-done/workflows/execute-plan.md
@/home/hybridz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/26-feature-engineering/26-03-SUMMARY.md

Key context:
- MODEL_FEATURES in src/data/features.py now has 31 features (was 25). The existing model scripts already import MODEL_FEATURES dynamically, so they will automatically pick up the expanded list.
- The existing pipeline scripts (baseline.py, lightgbm_xgboost.py, calibration.py) are well-structured and use MODEL_FEATURES throughout. They should work with 31 features without code changes.
- Optuna is stochastic — each run produces different results. Committed artifacts become authoritative.
- LightGBM early_stopping monitors LAST metric by default. Must use `early_stopping(50, first_metric_only=True)` to monitor `average_precision` (already correct in code).
- FrozenEstimator replaces cv='prefit' (already correct in calibration.py).
- Export raw LightGBM to ONNX, not calibrated wrapper (already correct).
- Gate tests 2.3 (ONNX) and 3.2 (SHAP) currently fail because models were trained with 25 features. Retraining will fix 2.3.

<interfaces>
From src/models/lightgbm_xgboost.py:
  run_lgbm_xgb_pipeline() -> dict  # Returns model_results dict
  - Loads enaho_with_features.parquet
  - Creates temporal splits (train 2018-21, val 2022, test 2023)
  - Runs Optuna for LightGBM (100 trials) and XGBoost (50 trials)
  - Saves model joblibs, predictions parquets, PR curves
  - Updates model_results.json with lightgbm + xgboost entries

From src/models/calibration.py:
  run_calibration_pipeline() -> dict  # Returns model_results dict
  - Loads model_lgbm.joblib
  - Platt scaling on validation set
  - ONNX export of raw LightGBM
  - Final test evaluation
  - Updates model_results.json with test_2023_calibrated entry

From src/models/baseline.py (shared utilities):
  create_temporal_splits(df) -> (train_df, val_df, test_df)
  _df_to_numpy(df) -> (X, y, w)
  compute_metrics(y_true, y_prob, y_pred, weights) -> dict
  _threshold_analysis(y_true, y_prob, w) -> dict
  TRAIN_YEARS, VALIDATE_YEAR, TEST_YEAR, ID_COLUMNS, FIXED_THRESHOLDS
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retrain LightGBM + XGBoost with 31-feature matrix</name>
  <files>data/processed/model_lgbm.joblib, data/processed/model_xgb.joblib, data/processed/predictions_lgbm.parquet, data/processed/predictions_xgb.parquet, data/exports/model_results.json, data/exports/figures/pr_curve_lgbm.png, data/exports/figures/pr_curve_xgb.png</files>
  <action>
Run the existing LightGBM + XGBoost pipeline which will automatically use the expanded 31-feature MODEL_FEATURES:

```bash
uv run python src/models/lightgbm_xgboost.py
```

The script already:
1. Imports MODEL_FEATURES from src/data/features.py (now 31 features)
2. Runs Optuna for LightGBM (100 trials) and XGBoost (50 trials)
3. Retrains best models with early stopping
4. Saves model_lgbm.joblib, model_xgb.joblib
5. Saves predictions_lgbm.parquet, predictions_xgb.parquet
6. Generates PR curve PNGs
7. Updates model_results.json with lightgbm + xgboost entries

No code changes should be needed — the pipeline uses MODEL_FEATURES dynamically.

**Before running**: Record the PREVIOUS val PR-AUC values from model_results.json for before/after comparison:
- Previous LightGBM val PR-AUC (weighted): read from model_results.json before overwrite
- Previous XGBoost val PR-AUC (weighted): read from model_results.json before overwrite

**After running**: Verify:
- model_results.json lightgbm.metadata.n_features == 31
- model_results.json lightgbm.metadata.feature_names has 31 entries
- No single feature has > 50% importance (script asserts this)
- Algorithm-independence ratio >= 0.95

**IMPORTANT**: This task will take ~20-30 minutes due to Optuna optimization. Run in background if needed.
  </action>
  <verify>
    <automated>python3 -c "
import json
with open('data/exports/model_results.json') as f:
    d = json.load(f)
lgbm = d['lightgbm']
assert lgbm['metadata']['n_features'] == 31, f'Expected 31 features, got {lgbm[\"metadata\"][\"n_features\"]}'
assert len(lgbm['metadata']['feature_names']) == 31
assert lgbm['metrics']['validate_2022']['weighted']['pr_auc'] > 0.14
xgb = d['xgboost']
assert xgb['metadata']['n_features'] == 31
ratio = xgb['metrics']['validate_2022']['weighted']['pr_auc'] / lgbm['metrics']['validate_2022']['weighted']['pr_auc']
assert ratio >= 0.95, f'Algorithm independence ratio {ratio:.4f} < 0.95'
print(f'LightGBM val PR-AUC: {lgbm[\"metrics\"][\"validate_2022\"][\"weighted\"][\"pr_auc\"]:.4f}')
print(f'XGBoost val PR-AUC: {xgb[\"metrics\"][\"validate_2022\"][\"weighted\"][\"pr_auc\"]:.4f}')
print(f'Features: {lgbm[\"metadata\"][\"n_features\"]}')
print('PASS')
"</automated>
  </verify>
  <done>LightGBM and XGBoost retrained with 31 features; model_results.json updated with new metrics; model joblibs, predictions parquets, and PR curves saved</done>
</task>

<task type="auto">
  <name>Task 2: Re-calibrate LightGBM and re-export ONNX</name>
  <files>data/processed/model_lgbm_calibrated.joblib, data/processed/predictions_lgbm_calibrated.parquet, data/exports/onnx/lightgbm_dropout.onnx, data/exports/figures/calibration_plot.png, data/exports/model_results.json</files>
  <action>
Run the existing calibration pipeline which will use the retrained model_lgbm.joblib from Task 1:

```bash
uv run python src/models/calibration.py
```

The script already:
1. Loads model_lgbm.joblib (retrained in Task 1)
2. Computes uncalibrated Brier score baseline
3. Applies Platt scaling (FrozenEstimator + CalibratedClassifierCV)
4. Extracts Platt A/B parameters
5. Exports raw LightGBM to ONNX (FloatTensorType with 31 features)
6. Validates ONNX predictions (max diff < 1e-4)
7. Evaluates on test 2023 (exactly once)
8. Saves calibration plot, calibrated predictions, calibrated model
9. Updates model_results.json with test_2023_calibrated entry

No code changes should be needed — the calibration script reads MODEL_FEATURES for ONNX input type sizing.

**After running**: Verify:
- Brier calibrated < Brier uncalibrated
- ONNX max diff < 1e-4
- Val-test PR-AUC gap < 0.07
- test_2023_calibrated entry exists in model_results.json
- ONNX file < 50 MB

**Record new Platt parameters**: These replace the previous A=-6.236085, B=4.442308 values.
  </action>
  <verify>
    <automated>python3 -c "
import json
with open('data/exports/model_results.json') as f:
    d = json.load(f)
cal = d['test_2023_calibrated']
assert cal['metadata']['platt_a'] is not None
assert cal['metadata']['platt_b'] is not None
assert cal['metadata']['brier_calibrated'] < cal['metadata']['brier_uncalibrated'], 'Calibration did not improve Brier'
assert cal['metadata']['onnx_max_abs_diff'] < 1e-4, f'ONNX diff {cal[\"metadata\"][\"onnx_max_abs_diff\"]} >= 1e-4'
val_prauc = cal['metrics']['validate_2022']['weighted']['pr_auc']
test_prauc = cal['metrics']['test_2023']['weighted']['pr_auc']
gap = abs(val_prauc - test_prauc)
assert gap < 0.07, f'Val-test gap {gap:.4f} >= 0.07'
from pathlib import Path
assert Path('data/exports/onnx/lightgbm_dropout.onnx').exists()
assert Path('data/processed/model_lgbm_calibrated.joblib').exists()
print(f'Platt A: {cal[\"metadata\"][\"platt_a\"]:.6f}')
print(f'Platt B: {cal[\"metadata\"][\"platt_b\"]:.6f}')
print(f'Brier improvement: {cal[\"metadata\"][\"brier_improvement_pct\"]:.1f}%')
print(f'ONNX max diff: {cal[\"metadata\"][\"onnx_max_abs_diff\"]:.2e}')
print(f'Val-test gap: {gap:.4f}')
print('PASS')
"</automated>
  </verify>
  <done>LightGBM re-calibrated with updated Platt A/B coefficients; ONNX re-exported and validated (max diff < 1e-4); test_2023_calibrated entry updated in model_results.json; calibrated predictions and model saved</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. model_results.json has lightgbm, xgboost, test_2023_calibrated entries all with n_features=31
2. ONNX file exists and is valid (max diff < 1e-4)
3. All model joblibs exist (model_lgbm.joblib, model_xgb.joblib, model_lgbm_calibrated.joblib)
4. All prediction parquets exist (predictions_lgbm.parquet, predictions_xgb.parquet, predictions_lgbm_calibrated.parquet)
5. PR curve PNGs and calibration plot exist
6. Gate test 2.3 should now pass: `uv run pytest tests/gates/test_gate_2_3.py -x`
</verification>

<success_criteria>
- LightGBM retrained with 31 features, Optuna best params logged
- XGBoost retrained with 31 features, algorithm-independence ratio >= 0.95
- Platt scaling re-applied with updated coefficients
- ONNX re-exported, max diff < 1e-4
- model_results.json updated with all new metrics
- Gate test 2.3 passes
</success_criteria>

<output>
After completion, create `.planning/phases/27-model-retraining/27-01-SUMMARY.md`
</output>
