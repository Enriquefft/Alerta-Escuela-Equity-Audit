---
phase: 27-model-retraining
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - src/models/baseline.py
  - data/processed/model_lr.joblib
  - data/processed/predictions_lr.parquet
  - data/processed/model_rf.joblib
  - data/processed/predictions_rf.parquet
  - data/processed/model_mlp.joblib
  - data/processed/predictions_mlp.parquet
  - data/exports/model_results.json
  - data/exports/figures/pr_curve_lr.png
  - data/exports/figures/pr_curve_rf.png
  - data/exports/figures/pr_curve_mlp.png
  - data/exports/prauc_before_after.json
  - tests/gates/test_gate_2_1.py
  - tests/gates/test_gate_2_2.py
  - tests/gates/test_gate_2_4.py
autonomous: true
requirements: [MODEL-02, MODEL-04]

must_haves:
  truths:
    - "LR retrained with 31 features, coefficients updated in model_results.json"
    - "RF retrained with 31 features via Optuna, val PR-AUC > 0.18"
    - "MLP retrained with 31 features via Optuna, val PR-AUC > 0.18"
    - "Before/after PR-AUC comparison table produced for all 5 models"
    - "Gate tests 2.1, 2.2, 2.4 pass with 31-feature models"
  artifacts:
    - path: "data/exports/prauc_before_after.json"
      provides: "Before/after PR-AUC comparison for all 5 model families"
    - path: "data/exports/model_results.json"
      provides: "Updated metrics for all 5 models (31 features)"
    - path: "data/processed/model_lr.joblib"
      provides: "Retrained LR model (31 features)"
    - path: "data/processed/model_rf.joblib"
      provides: "Retrained RF model (31 features)"
    - path: "data/processed/model_mlp.joblib"
      provides: "Retrained MLP model (31 features)"
  key_links:
    - from: "data/exports/prauc_before_after.json"
      to: "data/exports/model_results.json"
      via: "PR-AUC values extracted from model_results.json"
      pattern: "pr_auc"
    - from: "tests/gates/test_gate_2_1.py"
      to: "data/exports/model_results.json"
      via: "json.load assertion on n_features"
      pattern: "n_features"
---

<objective>
Retrain LR, RF, and MLP with the expanded 31-feature matrix, then produce a before/after PR-AUC comparison table for all 5 model families. Update gate tests for new feature count.

Purpose: Complete the model retraining for all 5 families (LR, LightGBM, XGBoost, RF, MLP). The before/after comparison is the key deliverable — it shows whether the 6 new features (overage, interactions) improve prediction, a finding that feeds Phase 29's interpretation.

Output: Updated model artifacts for LR/RF/MLP, prauc_before_after.json with 5-model comparison table, updated gate tests.
</objective>

<execution_context>
@/home/hybridz/.claude/get-shit-done/workflows/execute-plan.md
@/home/hybridz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/27-model-retraining/27-01-SUMMARY.md

Key context:
- Plan 01 already retrained LightGBM + XGBoost + calibrated. model_results.json has updated lightgbm/xgboost/test_2023_calibrated entries.
- Previous PR-AUC values (25-feature models) from MEMORY.md:
  - LR val PR-AUC (weighted): 0.2103
  - LightGBM val PR-AUC (weighted): 0.2611
  - XGBoost val PR-AUC (weighted): 0.2612
  - RF val PR-AUC (weighted): 0.2613
  - MLP val PR-AUC (weighted): 0.2380
- baseline.py creates model_results.json from scratch (overwrites). Must save LightGBM/XGBoost entries BEFORE running baseline.py, then merge back.
- MLP Optuna takes ~25 min. RF Optuna is faster (~10 min).
- class_weight='balanced' for LR and RF; scale_pos_weight for tree models.
- BSS negative for uncalibrated models — do not assert BSS > threshold.
- Gate tests need n_features assertions updated from 25 to 31.

<interfaces>
From src/models/baseline.py:
  run_baseline_pipeline() -> dict  # Returns model_results dict
  - WARNING: This OVERWRITES model_results.json with only logistic_regression key
  - Must save and restore other model entries after running

From src/models/random_forest.py:
  run_rf_pipeline() -> dict  # Adds random_forest to model_results.json

From src/models/mlp.py:
  run_mlp_pipeline() -> dict  # Adds mlp to model_results.json

Gate test files that reference n_features or hardcoded values:
  tests/gates/test_gate_2_1.py — LR baseline tests
  tests/gates/test_gate_2_2.py — LightGBM/XGBoost tests
  tests/gates/test_gate_2_4.py — RF/MLP/cross-model tests
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retrain LR, RF, and MLP with 31-feature matrix</name>
  <files>data/processed/model_lr.joblib, data/processed/predictions_lr.parquet, data/processed/model_rf.joblib, data/processed/predictions_rf.parquet, data/processed/model_mlp.joblib, data/processed/predictions_mlp.parquet, data/exports/model_results.json, data/exports/figures/pr_curve_lr.png, data/exports/figures/pr_curve_rf.png, data/exports/figures/pr_curve_mlp.png</files>
  <action>
**CRITICAL ORDER**: baseline.py OVERWRITES model_results.json. Must preserve Plan 01's entries.

**Step 1: Save existing model_results.json entries**
Before running baseline.py, save the current model_results.json (which has Plan 01's lightgbm, xgboost, test_2023_calibrated entries):
```python
import json
with open('data/exports/model_results.json') as f:
    saved = json.load(f)
# saved has: lightgbm, xgboost, test_2023_calibrated (from Plan 01)
```

**Step 2: Retrain LR**
```bash
uv run python src/models/baseline.py
```
This overwrites model_results.json with ONLY logistic_regression. After it completes, merge back Plan 01's entries:
```python
import json
with open('data/exports/model_results.json') as f:
    lr_results = json.load(f)
# Merge Plan 01's entries back
lr_results['lightgbm'] = saved['lightgbm']
lr_results['xgboost'] = saved['xgboost']
lr_results['test_2023_calibrated'] = saved['test_2023_calibrated']
with open('data/exports/model_results.json', 'w') as f:
    json.dump(lr_results, f, indent=2)
```

**Step 3: Retrain RF**
```bash
uv run python src/models/random_forest.py
```
This adds/updates `random_forest` key in model_results.json. Takes ~10 min.

**Step 4: Retrain MLP**
```bash
uv run python src/models/mlp.py
```
This adds/updates `mlp` key in model_results.json. Takes ~25 min. Run in background if needed.

**After all three**: Verify model_results.json has all 6 keys: logistic_regression, lightgbm, xgboost, test_2023_calibrated, random_forest, mlp. All should show n_features=31.

**Alternative approach** (if running steps manually is too error-prone): Write a small orchestration script that does all four steps atomically. This is preferred to avoid partial state.
  </action>
  <verify>
    <automated>python3 -c "
import json
with open('data/exports/model_results.json') as f:
    d = json.load(f)
expected_keys = {'logistic_regression', 'lightgbm', 'xgboost', 'test_2023_calibrated', 'random_forest', 'mlp'}
actual_keys = set(d.keys())
assert expected_keys.issubset(actual_keys), f'Missing keys: {expected_keys - actual_keys}'
for model in ['logistic_regression', 'lightgbm', 'xgboost', 'random_forest', 'mlp']:
    n = d[model]['metadata']['n_features']
    assert n == 31, f'{model} has n_features={n}, expected 31'
    prauc = d[model]['metrics']['validate_2022']['weighted']['pr_auc']
    assert prauc > 0.14, f'{model} val PR-AUC {prauc:.4f} < 0.14'
    print(f'{model}: val PR-AUC = {prauc:.4f}, n_features = {n}')
print(f'model_results.json keys: {sorted(d.keys())}')
print('PASS')
"</automated>
  </verify>
  <done>All 5 model families retrained with 31 features; model_results.json has all 6 keys with n_features=31 and valid PR-AUC values</done>
</task>

<task type="auto">
  <name>Task 2: Produce before/after PR-AUC comparison and update gate tests</name>
  <files>data/exports/prauc_before_after.json, tests/gates/test_gate_2_1.py, tests/gates/test_gate_2_2.py, tests/gates/test_gate_2_4.py</files>
  <action>
**Part A: Create before/after PR-AUC comparison table**

Create `data/exports/prauc_before_after.json` with the following structure:
```json
{
  "description": "PR-AUC comparison: 25-feature (v3) vs 31-feature (v4) models",
  "generated_at": "ISO timestamp",
  "previous_feature_count": 25,
  "new_feature_count": 31,
  "new_features": ["overage_years", "is_overage", "age_x_working", "age_x_poverty", "rural_x_parent_ed", "sec_age_x_income"],
  "models": {
    "logistic_regression": {
      "previous_val_prauc": 0.2103,
      "new_val_prauc": <from model_results.json>,
      "change": <new - previous>,
      "change_pct": <(new - prev) / prev * 100>
    },
    "lightgbm": {
      "previous_val_prauc": 0.2611,
      "new_val_prauc": <from model_results.json>,
      "change": ...,
      "change_pct": ...
    },
    "xgboost": {
      "previous_val_prauc": 0.2612,
      "new_val_prauc": <from model_results.json>,
      "change": ...,
      "change_pct": ...
    },
    "random_forest": {
      "previous_val_prauc": 0.2613,
      "new_val_prauc": <from model_results.json>,
      "change": ...,
      "change_pct": ...
    },
    "mlp": {
      "previous_val_prauc": 0.2380,
      "new_val_prauc": <from model_results.json>,
      "change": ...,
      "change_pct": ...
    }
  },
  "interpretation_note": "Positive change means new features improved prediction. Negative or near-zero change means the model found no predictive signal in the new features. Both outcomes are valid findings for the v4 experiment."
}
```

Read new PR-AUC values from model_results.json. Use the hardcoded previous values from MEMORY.md (these are the authoritative v3 baselines).

Print a formatted comparison table to stdout.

**Part B: Update gate tests for 31 features**

1. **test_gate_2_1.py**: If it hardcodes n_features=25 or checks feature_names length, update to 31. Check for any hardcoded PR-AUC thresholds that might need adjustment (keep them as minimum thresholds, don't raise them — the models might not improve with new features).

2. **test_gate_2_2.py**: Same pattern — update n_features references from 25 to 31. Keep PR-AUC minimum thresholds at current values (0.2103 for LightGBM-beats-LR check).

3. **test_gate_2_4.py**: Update n_features references and prediction row counts. The prediction parquet row count should stay at 52,112 (val + test = 26,477 + 25,635).

**IMPORTANT**: Only update hardcoded values that will break with 31 features. Do NOT change behavioral thresholds (PR-AUC minimums, importance concentration limits, etc.) — those should remain as-is since they are minimum quality bars.

After updates, run all three gate test files:
```bash
uv run pytest tests/gates/test_gate_2_1.py tests/gates/test_gate_2_2.py tests/gates/test_gate_2_4.py -x -v
```
  </action>
  <verify>
    <automated>uv run pytest tests/gates/test_gate_2_1.py tests/gates/test_gate_2_2.py tests/gates/test_gate_2_4.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>prauc_before_after.json created with 5-model comparison; gate tests 2.1, 2.2, 2.4 updated for 31 features and all pass; before/after table printed showing whether new features improved prediction</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. model_results.json has all 6 keys, all with n_features=31
2. prauc_before_after.json exists with 5-model before/after comparison
3. All model joblibs exist (lr, lgbm, xgb, rf, mlp, lgbm_calibrated)
4. All prediction parquets exist
5. Gate tests 2.1, 2.2, 2.3, 2.4 all pass:
   ```bash
   uv run pytest tests/gates/test_gate_2_1.py tests/gates/test_gate_2_2.py tests/gates/test_gate_2_3.py tests/gates/test_gate_2_4.py -x -v
   ```
</verification>

<success_criteria>
- All 5 model families retrained with 31 features
- model_results.json updated with new metrics for all models
- Before/after PR-AUC comparison table produced (prauc_before_after.json)
- Gate tests 2.1, 2.2, 2.4 pass with 31-feature models
- Clear comparison showing whether new features improve prediction
</success_criteria>

<output>
After completion, create `.planning/phases/27-model-retraining/27-02-SUMMARY.md`
</output>
