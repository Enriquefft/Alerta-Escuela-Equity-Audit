---
phase: 32-structural-tightening
plan: 02
type: execute
wave: 2
depends_on: [32-01]
files_modified: [paper/main.tex]
autonomous: true
requirements: [STRC-04]

must_haves:
  truths:
    - "The \begin{table}...\end{table} block for tab:enaho_siagie no longer exists in paper/main.tex"
    - "All in-text references to tab:enaho_siagie (Table~\ref{tab:enaho_siagie}) are removed from the body text"
    - "The physical file paper/tables/table_09_enaho_siagie.tex remains on disk (not deleted)"
    - "Paper compiles with latexmk without errors after removal — no undefined reference warnings for tab:enaho_siagie"
    - "The appendix contains 5 tables (was 6), and the removed table was Appendix Table 9 (ENAHO vs SIAGIE)"
  artifacts:
    - path: "paper/main.tex"
      provides: "Appendix tables section without tab:enaho_siagie block"
  key_links:
    - from: "Section 8 (Limitations), line 274"
      to: "tab:enaho_siagie reference removed"
      via: "Delete '(Table~\\ref{tab:enaho_siagie}, Appendix~\\ref{sec:appendix_tables})' from limitation sentence"
    - from: "Section 3 (Data), line 96"
      to: "tab:enaho_siagie reference removed"
      via: "Delete '(Table~\\ref{tab:enaho_siagie}, Appendix~\\ref{sec:appendix_tables})' from data sentence"
---

<objective>
Remove the ENAHO vs. SIAGIE comparison table (tab:enaho_siagie) from paper/main.tex entirely: delete the \begin{table}...\end{table} block in the appendix and all in-text \ref{tab:enaho_siagie} citations from body sections.

Purpose: This table is inferential/hypothetical (SIAGIE columns are inferred from public docs, not direct access), carries an awkward disclaimer in its caption, and invites reviewer scrutiny without contributing to the fairness findings. Removing it reduces appendix clutter.
Output: paper/main.tex with 5 appendix tables instead of 6, no broken references, clean compile.
</objective>

<execution_context>
@/home/hybridz/.claude/get-shit-done/workflows/execute-plan.md
@/home/hybridz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-structural-tightening/32-CONTEXT.md
@.planning/phases/32-structural-tightening/32-01-SUMMARY.md

<interfaces>
<!-- Exact locations in paper/main.tex where tab:enaho_siagie appears. -->

Appendix table block (lines 366–370):
  \begin{table}[H]
    \caption[ENAHO vs.\ SIAGIE Feature Availability]{ENAHO vs.\ SIAGIE Feature Availability.
      SIAGIE columns are inferred from public documentation \cite{minedu2023alerta,minedu2022estadistica};
      we have not accessed SIAGIE records directly.}\vspace*{1ex}
    \label{tab:enaho_siagie}
    \input{tables/table_09_enaho_siagie.tex}
  \end{table}
  → DELETE this entire block (lines 366–370 inclusive, plus blank lines around it).

In-text reference 1 — Section 3 Data (line 96):
  "...differ substantially from SIAGIE: our proxy model lacks daily attendance records, grade
  history, and multi-year student trajectories (Table~\ref{tab:enaho_siagie},
  Appendix~\ref{sec:appendix_tables})."
  → Remove only the parenthetical citation. Keep the sentence's factual claim intact:
  "...differ substantially from SIAGIE: our proxy model lacks daily attendance records, grade
  history, and multi-year student trajectories."

In-text reference 2 — Section 8 Limitations (line 274):
  "This paper audits a proxy model, not the actual Alerta Escuela system
  (Table~\ref{tab:enaho_siagie}, Appendix~\ref{sec:appendix_tables})."
  → Remove the parenthetical entirely:
  "This paper audits a proxy model, not the actual Alerta Escuela system."

Physical file to NOT delete:
  paper/tables/table_09_enaho_siagie.tex — leave on disk, only remove \input and \begin{table} block.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove tab:enaho_siagie table block and in-text references</name>
  <files>paper/main.tex</files>
  <action>
Make three surgical edits to paper/main.tex:

**Edit A — Delete the appendix table block (lines ~366–370).**
Delete the entire \begin{table}[H] ... \end{table} block that contains \label{tab:enaho_siagie}
and \input{tables/table_09_enaho_siagie.tex}. Include any surrounding blank lines that are part
of the table separation. Do not touch adjacent tables (tab:lr_coefficients before it,
tab:crossmodel_fnr after it).

**Edit B — Remove in-text reference in Section 3 (Data), ~line 96.**
Current text:
  "...and multi-year student trajectories (Table~\ref{tab:enaho_siagie}, Appendix~\ref{sec:appendix_tables}). However, the survey dimensions..."
Replace with (remove only the parenthetical, preserve the period and sentence flow):
  "...and multi-year student trajectories. However, the survey dimensions..."

**Edit C — Remove in-text reference in Section 8 (Limitations), ~line 274.**
Current text:
  "This paper audits a proxy model, not the actual Alerta Escuela system (Table~\ref{tab:enaho_siagie}, Appendix~\ref{sec:appendix_tables})."
Replace with:
  "This paper audits a proxy model, not the actual Alerta Escuela system."

Do not delete paper/tables/table_09_enaho_siagie.tex from disk.
Do not touch any other tables or sections.

After making all edits, verify no remaining \ref{tab:enaho_siagie} exist:
  grep "tab:enaho_siagie" paper/main.tex
Expected: zero results.
  </action>
  <verify>
    <automated>cd /home/hybridz/Projects/Alerta-Escuela-Equity-Audit/paper && latexmk -pdf -interaction=nonstopmode main.tex 2>&1 | grep -E "Warning|Error|Undefined" | head -20; echo "Exit: $?"</automated>
  </verify>
  <done>
- `grep "tab:enaho_siagie" paper/main.tex` returns zero results
- `ls paper/tables/table_09_enaho_siagie.tex` confirms file still exists on disk
- Appendix contains 5 table blocks (grep "\begin{table}" in appendix section returns 5)
- `latexmk` exits 0, no "undefined reference" warnings for tab:enaho_siagie
  </done>
</task>

</tasks>

<verification>
After completing the task:
1. `grep -c "tab:enaho_siagie" paper/main.tex` — must return 0
2. `ls paper/tables/table_09_enaho_siagie.tex` — file exists
3. `latexmk -pdf -interaction=nonstopmode paper/main.tex` — exits 0, no undefined refs
4. Count appendix tables: `grep -c "\\\\begin{table}" paper/main.tex` — should be reduced by 1 vs Plan 01 output
</verification>

<success_criteria>
No references to tab:enaho_siagie remain in main.tex. The .tex source file for the table is
preserved on disk. Paper compiles cleanly with no undefined reference warnings.
</success_criteria>

<output>
After completion, create `.planning/phases/32-structural-tightening/32-02-SUMMARY.md`
</output>
