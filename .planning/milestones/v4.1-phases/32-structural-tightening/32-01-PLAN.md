---
phase: 32-structural-tightening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [paper/main.tex]
autonomous: true
requirements: [STRC-01, STRC-02, STRC-03]

must_haves:
  truths:
    - "Section 6 has 3 subsections (Language Dimension, Intersectional Analysis, SHAP Interpretability) — the standalone 'Other Demographic Dimensions' subsection no longer exists as a separate heading"
    - "The content of the former 6.2 (minus the age sentence) is absorbed as a closing paragraph at the end of 6.1 (Language Dimension)"
    - "The sentence 'This FNR rank order holds across all five model families.' does not appear in Section 6.1 or Section 6.2 (former Other Demographic Dimensions)"
    - "Subsection labels and any cross-references to the former 6.3/6.4 now correctly point to the renumbered 6.2/6.3"
    - "Paper compiles with latexmk without errors after all changes"
  artifacts:
    - path: "paper/main.tex"
      provides: "Revised Section 6 with 3 subsections"
      contains: "\\subsection{Intersectional Analysis}"
  key_links:
    - from: "Section 6.1 closing paragraph"
      to: "Absorbed former 6.2 text"
      via: "Natural prose transition: 'Language dominates the fairness picture...'"
    - from: "Section labels in text (e.g., Section~\\ref{...})"
      to: "Correct subsection labels after renumbering"
      via: "\\label{} and \\ref{} matching"
---

<objective>
Fold Section 6.2 (Other Demographic Dimensions) into Section 6.1 (Language Dimension) and remove the duplicate cross-architecture sentence from Section 6.1.

Purpose: Eliminate standalone-section bloat for a paragraph that does not need its own heading, and remove a robustness claim that already lives in Section 5.2 (Predictive Validity).
Output: Section 6 with 3 subsections (renumbered), paper compiling cleanly.
</objective>

<execution_context>
@/home/hybridz/.claude/get-shit-done/workflows/execute-plan.md
@/home/hybridz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-structural-tightening/32-CONTEXT.md

<interfaces>
<!-- Key content in paper/main.tex at the time of planning. Executor uses these directly. -->

Section 6 subsection structure (lines ~201–233):
  Line 201: \subsection{Language Dimension: The Surveillance--Invisibility Axis}
  Line 217: \subsection{Other Demographic Dimensions}     ← REMOVE this heading
  Line 219: [one-paragraph content of 6.2]                ← KEEP minus age sentence, absorb into 6.1
  Line 221: \subsection{Intersectional Analysis}          ← becomes 6.2 (renumber in LaTeX = automatic)
  Line 233: \subsection{SHAP Interpretability}            ← becomes 6.3 (renumber = automatic)

Current 6.2 paragraph (line 219, verbatim):
  "Language dominates: region shows FNR variation driven by spatial profiles, poverty quintiles
  show a monotonic flagging gradient that partially tracks base rates, and the sex gap is minimal
  (FNR difference of 0.026). Nationality ($n=27$ non-Peruvian) is unusable for inference.
  Older students (ages 15--17) are flagged more accurately than younger students, reflecting
  both higher base rates and the model's reliance on age as a predictive feature."

Age sentence to DROP (do not include in absorbed paragraph):
  "Older students (ages 15--17) are flagged more accurately than younger students, reflecting
  both higher base rates and the model's reliance on age as a predictive feature."

Cross-arch sentence to verify absent / remove from Section 6.1 area:
  "This FNR rank order holds across all five model families."
  (CONTEXT.md says to remove it from 6.1; verify it is not present in lines 201–220 of the
  current file. If absent, no action needed for STRC-02.)

STRC-03 note: Abstract v2 mention was already removed in Phase 31. No tasks needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fold Section 6.2 into 6.1 and remove cross-arch duplicate</name>
  <files>paper/main.tex</files>
  <action>
Make the following surgical edits to paper/main.tex in order:

**Edit A — Remove the \subsection{Other Demographic Dimensions} heading (line 217).**
Delete the line:
  \subsection{Other Demographic Dimensions}

**Edit B — Replace the 6.2 paragraph with an absorbed version at the end of 6.1.**
The current 6.2 paragraph (line 219) reads:
  "Language dominates: region shows FNR variation driven by spatial profiles, poverty quintiles
  show a monotonic flagging gradient that partially tracks base rates, and the sex gap is minimal
  (FNR difference of 0.026). Nationality ($n=27$ non-Peruvian) is unusable for inference.
  Older students (ages 15--17) are flagged more accurately than younger students, reflecting
  both higher base rates and the model's reliance on age as a predictive feature."

Replace with (drop the age sentence; use discretion to add a minimal bridge if needed):
  "Language dominates the fairness picture: region shows FNR variation driven by spatial profiles,
  poverty quintiles show a monotonic flagging gradient that partially tracks base rates, and the
  sex gap is minimal (FNR difference of 0.026). Nationality ($n=27$ non-Peruvian) is unusable
  for inference."

Move this absorbed paragraph to immediately after line 215 (the last sentence of 6.1, currently
"Figure~\ref{fig:fnr_fpr} visualizes this axis..."), placing it before the \subsection{Intersectional Analysis} line. The resulting structure in Section 6 will be:

  6.1 \subsection{Language Dimension: The Surveillance--Invisibility Axis}
      [existing 6.1 paragraphs]
      Figure~\ref{fig:fnr_fpr} visualizes this axis: ...
      [NEW absorbed paragraph — Language dominates the fairness picture: ...]

  6.2 \subsection{Intersectional Analysis}   ← was 6.3
  6.3 \subsection{SHAP Interpretability}     ← was 6.4

LaTeX auto-numbers subsections, so no manual numbering changes are needed. However, verify that
no \ref{} or cross-references in the text point to a label that was on the removed \subsection
heading. Search for any \label{} that was inside the former 6.2 — if none existed, no action
needed.

**Edit C — Verify and (if present) remove cross-arch sentence from Section 6.1.**
Search lines 201–220 of main.tex for the exact sentence:
  "This FNR rank order holds across all five model families."
If found in that range, delete it. If not found (it appears to be absent from Section 6 in the
current file), no action needed.

Do not touch any other sections.
  </action>
  <verify>
    <automated>cd /home/hybridz/Projects/Alerta-Escuela-Equity-Audit/paper && latexmk -pdf -interaction=nonstopmode main.tex 2>&1 | tail -5</automated>
  </verify>
  <done>
- `grep -n "Other Demographic" paper/main.tex` returns no matches (subsection heading removed)
- `grep -n "ages 15--17" paper/main.tex` returns no matches (age sentence removed)
- `grep -n "subsection{Intersectional" paper/main.tex` still matches (section still present)
- `grep -n "subsection{SHAP" paper/main.tex` still matches (section still present)
- `latexmk` exits 0, no undefined reference warnings for Section 6 cross-refs
  </done>
</task>

</tasks>

<verification>
After completing the task:
1. `grep -c "subsection" paper/main.tex` — Section 6 should have 3 subsections (down from 4)
2. `grep -n "Other Demographic" paper/main.tex` — no results
3. `grep -n "ages 15--17" paper/main.tex` — no results
4. `latexmk -pdf -interaction=nonstopmode paper/main.tex` — exits 0, no undefined refs
</verification>

<success_criteria>
Section 6 contains exactly 3 subsections. The former 6.2 paragraph content (minus age sentence)
appears at the end of 6.1 as a closing paragraph. The cross-architecture sentence is absent from
Section 6.1. Paper compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/32-structural-tightening/32-01-SUMMARY.md`
</output>
